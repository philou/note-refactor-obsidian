build:
  - os: [darwin, linux, windows]
    arch: ["386", amd64, arm64]
    command: npm
    arguments: [run, build]
test:
  - os: [darwin, linux, windows]
    arch: ["386", amd64, arm64]
    command: yarn 
    arguments: [run, test]
test-result-dir: test_results

